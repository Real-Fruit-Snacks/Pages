NAME

sane-hpljm1005
- SANE backend for Hewlett-Packard LaserJet M1005 MFP
Scanner

DESCRIPTION

The
sane-hpljm1005 library implements a SANE (Scanner
Access Now Easy) backend that provides access to the
following Hewlett-Packard scanner:

LaserJet
M1005

If you own a
scanner other than the ones listed above that works with
this backend, please let us know this by sending the
scanner&rsquo;s exact model name and the USB vendor and
device ids (e.g. from /proc/bus/usb/devices,
sane-find-scanner or syslog) to us. Even if the
scanner&rsquo;s name is only slightly different from the
models mentioned above, please let us know.

CONFIGURATION

None
required.

FILES

/usr/lib/x86_64-linux-gnu/sane/libsane-hpljm1005.a

The static library implementing
this backend.

/usr/lib/x86_64-linux-gnu/sane/libsane-hpljm1005.so

The shared library implementing
this backend (present on systems that support dynamic
loading).

SEE ALSO

sane(7),
sane-usb(5),

AUTHOR

Philippe
R&eacute;tornaz couriousous [AT] mandriva.org>