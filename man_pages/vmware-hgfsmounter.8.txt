NAME

vmware&minus;hgfsmounter
&minus; program for mounting HGFS shares

SYNOPSIS

mount.vmhgfs
SHARE DIRECTORY [ &minus;o OPTIONS ] 
vmware&minus;hgfsmounter SHARE DIRECTORY [
&minus;o OPTIONS ]

DESCRIPTION

This is a
console&minus;based mount helper application. It mounts the
HGFS share, specified by name, to a local directory. Share
names must be in host:dir format.

OPTIONS

uid=ARGUMENT

mount owner (by uid or
username)

gid=ARGUMENT

mount group (by gid or
groupname)

fmask=ARGUMENT

file umask (in octal)

dmask=ARGUMENT

directory umask (in octal)

ro

mount read&minus;only

rw

mount read&minus;write (default)

nosuid

ignore suid/sgid bits

suid

allow suid/sgid bits
(default)

nodev

prevent device node access

dev

allow device node access (default)

noexec

prevent program execution

exec

allow program execution
(default)

sync

file writes are synchronous

async

file writes are asynchronous
(default)

mand

allow mandatory locks

nomand

prevent mandatory locks
(default)

noatime

do not update access times

atime

update access times
(default)

nodiratime

do not update directory access
times

adirtime

update directory access times
(default)

ttl=ARGUMENT

time before file attributes
must be revalidated (in seconds). Improves performance but
decreases coherency. Defaults to 1 if not set.

EXAMPLES

This command is
intended to be run from within /bin/mount by passing the
option &rsquo;&minus;t vmhgfs&rsquo;. For example:

mount &minus;t
vmhgfs .host:/ /mnt/hgfs/ 
mount &minus;t vmhgfs .host:/foo /mnt/foo 
mount &minus;t vmhgfs .host:/foo/bar /var/lib/bar

SEE ALSO

vmware&minus;checkvm(1)

vmware&minus;hgfsclient(1) 
vmware&minus;toolbox(1) 
vmware&minus;toolbox&minus;cmd(1) 
vmware&minus;user(1) 
vmware&minus;xferlogs(1) 
libguestlib(3) 
libvmtools(3) 
vmware&minus;guestd(8) 
vmware&minus;user&minus;suid&minus;wrapper(8) 
vmblock(9) 
vmci(9) 
vmhgfs(9) 
vmmemctl(9) 
vmsock(9) 
vmsync(9) 
vmxnet(9) 
vmxnet3(9)

HOMEPAGE

More
information about vmware&minus;hgfsmounter and the Open VM
Tools can be found at
http://open&minus;vm&minus;tools.sourceforge.net/>.

AUTHOR

Open VM Tools
were written by VMware, Inc.
http://www.vmware.com/>.

This manual
page was put together from homepage materials by Daniel
Baumann daniel [AT] debian.org>, for the Debian
project (but may be used by others).