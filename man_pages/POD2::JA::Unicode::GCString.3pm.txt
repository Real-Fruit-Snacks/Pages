POD2::JA::Unicode::GCSUsergContributed Perl DoPOD2::JA::Unicode::GCString(3pm)

NAME
       Unicode::GCString~[ja] - UAX #29

SYNOPSIS
           use Unicode::GCString;
           $gcstring = Unicode::GCString->new($string);

DESCRIPTION
       Unicode::GCString UnicodeUnicode29 [UAX #29] extended grapheme cluster

       grapheme clusterUnicodegrapheme basegrapheme extender/"prepend"
       character

       new (STRING, [KEY => VALUE, ...])
       new (STRING, [LINEBREAK])
            Unicode STRING (Unicode::GCString )

           KEY => VALUE  "" in Unicode::LineBreak~[ja] Unicode::LineBreak~[ja]
           LINEBREAK

           :  2012.10

       copy

       chars
            UnicodeUnicode

       columns
             "DESCRIPTION" in Unicode::LineBreak~[ja]

       length

       as_string
       """OBJECT"""
            Unicode

       cmp (STRING)
       STRING "cmp" STRING
            Unicode

       concat (STRING)
       STRING "." STRING
            STRING Unicode  (columns() )  (length() )

       join ([STRING, ...])
            STRING STRING  Unicode

       substr (OFFSET, [LENGTH, [REPLACEMENT]])
            OFFSET  LENGTH REPLACEMENT REPLACEMENT  Unicode

           Note:  substr()

       as_array
       "@{"OBJECT"}"
       as_arrayref

       eos

       item ([OFFSET])
            OFFSET OFFSET

       next
       "<"OBJECT">"

       pos ([OFFSET])
            OFFSET

       lbc  (Unicode::LineBreak~[ja] )

       lbcext
            (Unicode::LineBreak~[ja] )  CM

CAVEATS
       o    ()

       o   Perl  5.10.1 Unicode::GCString  Unicode  "utf8_mg_pos_cache_update"

               $sub = substr($gcstring, $i, $j);

               $sub = substr("$gcstring", $i, $j);

               $sub = substr($gcstring->as_string, $i, $j);

       o    tailoring

VERSION
       $VERSION

       2013.10
           o   new() Unicode iso-8859-1 (Latin 1) Unicode

SEE ALSO
       [UAX #29] Mark Davis (ed.) (2009-2013).  Unicode Standard Annex #29:
       Unicode Text Segmentation, Revisions 15-23.
       <http://www.unicode.org/reports/tr29/>.

AUTHOR
       Hatuka*nezumi - IKEDA Soji <hatuka(at)nezumi.nu>

COPYRIGHT
       Copyright (C) 2009-2013 Hatuka*nezumi - IKEDA Soji.

       This program is free software; you can redistribute it and/or modify it
       under the same terms as Perl itself.

perl v5.36.0                      2022-10-20  POD2::JA::Unicode::GCString(3pm)
