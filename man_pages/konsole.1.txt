NAME

Konsole
&minus; X terminal emulator for KDE.

SYNOPSIS

Konsole
[Qt&minus;options] [KDE&minus;options] [options] [args]

DESCRIPTION

Konsole is an X
terminal emulation which provides a command-line interface
(CLI) while using the graphical K Desktop Environment.
Konsole helps to better organize user&rsquo;s desktop by
containing multiple sessions in a single window (a less
cluttered desktop).

Konsole
advanced features include simple configuration and the
ability to use multiple terminal shells in a single window,
making for a less cluttered desktop.

Using Konsole,
a user can open:

Linux console
sessions

Shell
sessions

Screen
sessions

Midnight
Commander file manager sessions

Root console
sessions

Root Midnight
Commander sessions

User created
sessions

These sessions
can be renamed to help you keep track of all your shells, or
signaled (STOP, CONT, HUP, INT, TERM, KILL).

For more
control over konsole, a user can: hide/show the menubar
and/or frame; select the size of a konsole window, fonts,
color schemes, and key mapping; change location of the
scrollbar or hide the scrollbar; change location of the
tabbar or hide the tabbar.

All chosen
settings can be made the default for forthcoming sessions by
saving them.

OPTIONS

Konsole
specific options: 
&minus;&minus;name 

Set window class

&minus;&minus;ls

Start login shell &minus;T  Set the
window title

&minus;&minus;tn

Specify terminal type as set in
the TERM [xterm] environment variable

&minus;&minus;noclose

Do not close Konsole when
command exits

&minus;&minus;nohist

Do not save lines in
history

&minus;&minus;nomenubar

Do not display menubar

&minus;&minus;notabbar,
&minus;&minus;notoolbar

Do not display tab bar

&minus;&minus;noframe

Do not display frame

&minus;&minus;noscrollbar

Do not display scrollbar

&minus;&minus;noxft

Do not use Xft
(anti-aliasing)

&minus;&minus;vt_sz
CCxLL

Terminal size in columns x
lines

&minus;&minus;noresize

Terminal size is fixed

&minus;&minus;type

Start with given session
type

&minus;&minus;types

List available session
types

&minus;&minus;keytab

Set keytab to
&rsquo;name&rsquo;

&minus;&minus;keytabs

List available keytabs

&minus;&minus;profile

Start with given session
profile

&minus;&minus;profiles

List available session
profiles

&minus;&minus;schema

|  Set schema to
&rsquo;name&rsquo; or use &rsquo;file&rsquo;

&minus;&minus;schemas,
&minus;&minus;schemata

List available schemata

&minus;&minus;script

Enable extended DCOP Qt
functions

&minus;&minus;workdir

Change working directory to
&rsquo;dir&rsquo;

&minus;e  [
arguments ]

Execute &rsquo;command&rsquo;
instead of shell. It also sets the window title and icon
name to be the basename of the program being executed if
neither -T nor -n are given on the command line. This must
be the last option on the command line.

Arguments:

args

Arguments for &rsquo;command&rsquo;

Generic
options:

&minus;&minus;help

Show help about options

&minus;&minus;help&minus;qt

Show Qt specific options

&minus;&minus;help&minus;kde

Show KDE specific options

&minus;&minus;help&minus;all

Show all options

&minus;&minus;author

Show author information

&minus;v,
&minus;&minus;version

Show version information

&minus;&minus;license

Show license information

&minus;&minus;

End of options

SEE ALSO

Full user
documentation is available through the KDE Help Center. You
can also enter the URL help:/konsole/ directly into
konqueror or you can run &rsquo;khelpcenter
help:/konsole/&rsquo; from the command-line.

Konsole
homepage: http://konsole.kde.org/

AUTHORS

Kurt V.
Hindenburg kurt.hindenburg [AT] kdemail.net>

Lars Doelle
lars.doelle [AT] on-line.de>

OTHER

This manual
page was written by Ana Beatriz Guerrero Lopez
ana [AT] ekaia.org> for the Debian system (but may be used
by others).