BTXLD(8) BSD System Manager&rsquo;s Manual BTXLD(8)

NAME

btxld &mdash; link editor
for BTX clients

SYNOPSIS

btxld [&minus;qv]
[&minus;b file]
[&minus;E address]
[&minus;e address]
[&minus;f format]
[&minus;l file]
[&minus;o filename]
[&minus;P page]
[&minus;W page] file

DESCRIPTION

The btxld utility binds
the specified client executable together with a BTX loader
program and the BTX kernel, and creates a composite object
file suitable for loading during the boot process.

The options
are:

&minus;q

Quiet: inhibit
warnings.

&minus;v

Verbose:
display information about the files processed.

&minus;b file

Specify the BTX kernel to be
bound with the client.

&minus;E
address

Set the client entry point.

&minus;e
address

Set the BTX loader entry
point.

&minus;f
format

Specify the output format,
where format is one of &rsquo;bin&rsquo;,
&rsquo;aout&rsquo;, or &rsquo;elf&rsquo;.

&minus;l file

Specify the BTX loader to be
bound with the client.

&minus;o
filename

Name the output file. The
default is &rsquo;&rsquo;a.out&rsquo;&rsquo;.

&minus;P page

Specify the first page of the
client&rsquo;s segment to be marked &rsquo;present&rsquo;,
where page may be 0 or 1.

&minus;W page

Specify the first page of the
client&rsquo;s segment to be marked &rsquo;writable&rsquo;,
where page may be 0, and should not exceed the number
of pages occupied by the combined .text and .data segments
of the client image.

SEE ALSO

ld(1), boot(8)

DIAGNOSTICS

The btxld utility
exits 0 on success, and >0 if an error
occurs.

AUTHORS

Robert Nordier
rnordier [AT] FreeBSD.org>.

BSD
September 12, 1998 BSD