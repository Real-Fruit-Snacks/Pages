NAME

xfv −
viewer for frame-by frame view under X

SYNOPSIS

xfv
[options] parameter-file

DESCRIPTION

Vstream is a
collection of utilities for capturing reasonably high
quality video from a v4l device (specifically configured for
bttv). A simultaneous audio track is also captured.

This utility -
xfv - is a viewer for frame-by frame viewer of edited
streams under X windows (for testing/aligning edits).

OPTIONS

-double

display images twice normal
size.

parameter-file

the parameter file.

SEE ALSO

audioclip(1),
audioconv(1), str2ppm(1), stream(1),
ppm2str(1), xsf(1).

AUTHOR

This manual
page was written by Lenart Janos ocsi [AT] debian.org>,
for the Debian GNU system (but may be used by others).