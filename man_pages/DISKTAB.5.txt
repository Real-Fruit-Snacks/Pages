DISKTAB(5) BSD File Formats Manual DISKTAB(5)

NAME

disktab &mdash; disk
description file

SYNOPSIS

#include

DESCRIPTION

Disktab is a simple
database which describes disk geometries and disk partition
characteristics. It is used to initialize the disk label on
the disk. The format is patterned after the termcap(5)
terminal data base. Entries in disktab consist of a
number of &rsquo;:&rsquo;-separated fields. The first field
for each entry gives the names by which a disk&rsquo;s entry
may be selected, separated by &rsquo;|&rsquo; characters.
The last name given should be a long name fully identifying
the disk.

The optional
fields for each entry are:

ID

Type

Description

ty

str

Type of disk (e.g. removable, winchester)

dt

str

Type of controller (e.g. SMD, ESDI, floppy)

ns

num

Number of sectors per track

nt

num

Number of tracks per cylinder

nc

num

Total number of cylinders on the disk

sc

num

Number of sectors per cylinder, nc*nt default

su

num

Number of sectors per unit, sc*nc default

se

num

Sector size in bytes, DEV_BSIZE default

sf

bool

Controller supports bad144-style bad sector
forwarding 

rm

num

Rotation speed, rpm, 3600 default

sk

num

Sector skew per track, default 0

cs

num

Sector skew per cylinder, default 0

hs

num

Headswitch time, usec, default 0

ts

num

One-cylinder seek time, usec, default 0

il

num

Sector interleave (n:1), 1 default

d[0-4]

num

Drive-type-dependent parameters

bs

num

Boot block size, default BBSIZE

b[0-1]

num

Boot block filenames; see bsdlabel(8)

sb

num

Superblock size, default SBSIZE

ba

num

Block size for partition &rsquo;a&rsquo; (bytes)

bd

num

Block size for partition &rsquo;d&rsquo; (bytes)

be

num

Block size for partition &rsquo;e&rsquo; (bytes)

bf

num

Block size for partition &rsquo;f&rsquo; (bytes)

bg

num

Block size for partition &rsquo;g&rsquo; (bytes)

bh

num

Block size for partition &rsquo;h&rsquo; (bytes)

fa

num

Fragment size for partition &rsquo;a&rsquo; (bytes)

fd

num

Fragment size for partition &rsquo;d&rsquo; (bytes)

fe

num

Fragment size for partition &rsquo;e&rsquo; (bytes)

ff

num

Fragment size for partition &rsquo;f&rsquo; (bytes)

fg

num

Fragment size for partition &rsquo;g&rsquo; (bytes)

fh

num

Fragment size for partition &rsquo;h&rsquo; (bytes)

oa

num

Offset of partition &rsquo;a&rsquo; in sectors

ob

num

Offset of partition &rsquo;b&rsquo; in sectors

oc

num

Offset of partition &rsquo;c&rsquo; in sectors

od

num

Offset of partition &rsquo;d&rsquo; in sectors

oe

num

Offset of partition &rsquo;e&rsquo; in sectors

of

num

Offset of partition &rsquo;f&rsquo; in sectors

og

num

Offset of partition &rsquo;g&rsquo; in sectors

oh

num

Offset of partition &rsquo;h&rsquo; in sectors

pa

num

Size of partition &rsquo;a&rsquo; in sectors

pb

num

Size of partition &rsquo;b&rsquo; in sectors

pc

num

Size of partition &rsquo;c&rsquo; in sectors

pd

num

Size of partition &rsquo;d&rsquo; in sectors

pe

num

Size of partition &rsquo;e&rsquo; in sectors

pf

num

Size of partition &rsquo;f&rsquo; in sectors

pg

num

Size of partition &rsquo;g&rsquo; in sectors

ph

num

Size of partition &rsquo;h&rsquo; in sectors

ta

str

Partition type of partition &rsquo;a&rsquo; (4.2BSD file
system, swap, etc)

tb

str

Partition type of partition &rsquo;b&rsquo;

tc

str

Partition type of partition &rsquo;c&rsquo;

td

str

Partition type of partition &rsquo;d&rsquo;

te

str

Partition type of partition &rsquo;e&rsquo;

tf

str

Partition type of partition &rsquo;f&rsquo;

tg

str

Partition type of partition &rsquo;g&rsquo;

th

str

Partition type of partition &rsquo;h&rsquo;

FILES 
/etc/disktab 
SEE ALSO

getdiskbyname(3), disklabel(5),
disklabel(8), newfs(8)

HISTORY

The disktab description
file appeared in 4.2BSD.

BSD June 5,
1993 BSD