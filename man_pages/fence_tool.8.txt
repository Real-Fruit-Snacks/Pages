NAME

fence_tool
&minus; a utility for the fenced daemon

SYNOPSIS

fence_tool
[COMMAND] [OPTIONS]

DESCRIPTION

This utility
controls and queries the fenced(8) daemon with the
following commands:

join

join the fence domain.

leave

leave the fence domain.

dump

print the fenced internal debug buffer ont stdout.

ls

display internal fenced state.

The leave
command will not be sent to fenced if fence_tool detects
that any instances of gfs or dlm are in use.

OPTIONS

&minus;n

Show all node information in
ls. 

&minus;t
seconds

Retry cman connection for this
many seconds. 0 none, -1 indefinite. Default 0.

&minus;q
seconds

Delay join up to this many
seconds for the cluster to have quorum. 0 none, -1
indefinite. Default 0.

&minus;m
seconds

Delay join up to this many
seconds for all nodes in cluster.conf to be cluster members.
0 none, -1 indefinite. Default 0.

&minus;w
seconds

Wait up to this many seconds
for the result of join or leave. 0 none, -1 indefinite.
Default 0.

&minus;h

Print a help message describing available options, then
exit. 

&minus;V

Print program version information, then exit.

SEE ALSO

fenced(8)